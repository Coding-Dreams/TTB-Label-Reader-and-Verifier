OLLAMA_BASE_URL=http://ollama:11434

# Password protecting the web UI. Leave empty to disable auth.
APP_PASSWORD=
# Long random string so sessions survive container restarts.
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=
